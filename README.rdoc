== README

RSPEC tests used to test model methods and requests

Sidekiq used to import data in async mode in background

To respond with player data active model serialization used

To translit cyrilic to latin russian gem used

some to do work:
* authentication
* api versioning
* if sidekiq pro exists run imports in batch and add interface to check status of import
